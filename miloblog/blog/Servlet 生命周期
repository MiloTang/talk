  
	package servlet;<br><br>import java.io.IOException;<br><br>import javax.servlet.ServletConfig;<br>import javax.servlet.ServletException;<br>import javax.servlet.annotation.WebServlet;<br>import javax.servlet.http.HttpServlet;<br>import javax.servlet.http.HttpServletRequest;<br>import javax.servlet.http.HttpServletResponse;<br><br>/**<br>&nbsp;* Servlet implementation class ServletLife1<br>&nbsp;*/<br>@WebServlet(urlPatterns="/ServletLife1",loadOnStartup=1)<br>public class ServletLife1 extends HttpServlet {<br>&nbsp;&nbsp; &nbsp;private static final long serialVersionUID = 1L;<br>&nbsp;&nbsp; &nbsp;<br>&nbsp;&nbsp;&nbsp; /**<br>&nbsp;&nbsp;&nbsp;&nbsp; * @see HttpServlet#HttpServlet()<br>&nbsp;&nbsp;&nbsp;&nbsp; */<br>&nbsp;&nbsp;&nbsp; public ServletLife1() {<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; super();<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; System.out.println("构造方法被执行1");<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; // TODO Auto-generated constructor stub<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp;&nbsp; @Override<br>&nbsp;&nbsp;&nbsp; public void init() throws ServletException {<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;// TODO Auto-generated method stub<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;super.init();<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;System.out.println("初始化方法被执1");<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp;&nbsp; @Override<br>&nbsp;&nbsp;&nbsp; public void init(ServletConfig config) throws ServletException {<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;// TODO Auto-generated method stub<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;super.init(config);<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;System.out.println("初始化config被执行1");<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp; &nbsp;/**<br>&nbsp;&nbsp; &nbsp; * @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)<br>&nbsp;&nbsp; &nbsp; */<br>&nbsp;&nbsp; &nbsp;protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;// TODO Auto-generated method stub<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;System.out.println("get方法被执行1");<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;response.getWriter().append("Served at: ").append(request.getContextPath());<br>&nbsp;&nbsp; &nbsp;}<br><br>&nbsp;&nbsp; &nbsp;/**<br>&nbsp;&nbsp; &nbsp; * @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)<br>&nbsp;&nbsp; &nbsp; */<br>&nbsp;&nbsp; &nbsp;protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;// TODO Auto-generated method stub<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;System.out.println("post方法被执行1");<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;doGet(request, response);<br>&nbsp;&nbsp; &nbsp;}<br>&nbsp;&nbsp; &nbsp;<br>&nbsp;&nbsp; &nbsp;@Override<br>&nbsp;&nbsp; &nbsp;public void destroy() {<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;// TODO Auto-generated method stub<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;super.destroy();<br>&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; &nbsp;System.out.println("销毁方法被执行1");<br>&nbsp;&nbsp; &nbsp;}<br><br><br>}<br><br>package servlet;<br><br>import java.io.IOException;<br><br>import javax.servlet.ServletConfig;<br>import javax.servlet.ServletException;<br>import javax.servlet.annotation.WebServlet;<br>import javax.servlet.http.HttpServlet;<br>import javax.servlet.http.HttpServletRequest;<br>import javax.servlet.http.HttpServletResponse;<br><br>/**<br>&nbsp;* Servlet implementation class ServletLife<br>&nbsp;*/<br>@WebServlet(urlPatterns="/ServletLife",loadOnStartup=2)<br>public class ServletLife extends HttpServlet {<br>&nbsp;&nbsp;&nbsp; private static final long serialVersionUID = 1L;<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; <br>&nbsp;&nbsp;&nbsp; /**<br>&nbsp;&nbsp;&nbsp;&nbsp; * @see HttpServlet#HttpServlet()<br>&nbsp;&nbsp;&nbsp;&nbsp; */<br>&nbsp;&nbsp;&nbsp; public ServletLife() {<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; super();<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; System.out.println("构造方法被执行");<br>&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp;&nbsp; // TODO Auto-generated constructor stub<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp;&nbsp; @Override<br>&nbsp;&nbsp;&nbsp; public void init() throws ServletException {<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; // TODO Auto-generated method stub<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; super.init();<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; System.out.println("初始化方法被执");<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp;&nbsp; @Override<br>&nbsp;&nbsp;&nbsp; public void init(ServletConfig config) throws ServletException {<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; // TODO Auto-generated method stub<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; super.init(config);<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; System.out.println("初始化config被执行");<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp;&nbsp; /**<br>&nbsp;&nbsp;&nbsp; &nbsp;* @see HttpServlet#doGet(HttpServletRequest request, HttpServletResponse response)<br>&nbsp;&nbsp;&nbsp; &nbsp;*/<br>&nbsp;&nbsp;&nbsp; protected void doGet(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; // TODO Auto-generated method stub<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; System.out.println("get方法被执行");<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; response.getWriter().append("Served at: ").append(request.getContextPath());<br>&nbsp;&nbsp;&nbsp; }<br><br>&nbsp;&nbsp;&nbsp; /**<br>&nbsp;&nbsp;&nbsp; &nbsp;* @see HttpServlet#doPost(HttpServletRequest request, HttpServletResponse response)<br>&nbsp;&nbsp;&nbsp; &nbsp;*/<br>&nbsp;&nbsp;&nbsp; protected void doPost(HttpServletRequest request, HttpServletResponse response) throws ServletException, IOException {<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; // TODO Auto-generated method stub<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; System.out.println("post方法被执行");<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; doGet(request, response);<br>&nbsp;&nbsp;&nbsp; }<br>&nbsp;&nbsp;&nbsp; <br>&nbsp;&nbsp;&nbsp; @Override<br>&nbsp;&nbsp;&nbsp; public void destroy() {<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; // TODO Auto-generated method stub<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; super.destroy();<br>&nbsp;&nbsp;&nbsp; &nbsp;&nbsp;&nbsp; System.out.println("销毁方法被执行");<br>&nbsp;&nbsp;&nbsp; }<br><br>}<br>启动输出<br>构造方法被执行1<br>初始化方法被执1<br>初始化config被执行1<br>构造方法被执行<br>初始化方法被执<br>初始化config被执行<br><br>访问执行doGet或者doPost方法<br><br>重启服务器输出<br>销毁方法被执行<br>销毁方法被执行1<br>构造方法被执行1<br>初始化方法被执1<br>初始化config被执行1<br>构造方法被执行<br>初始化方法被执<br>初始化config被执行<br>